/*exported handleClassWindow */

function handleClassWindow($classWindow, dataChangeFunc, callName) {
	"use strict";
	var that;
	that = function(data){
		// handle classWindow stuff
	};
	return that;
}