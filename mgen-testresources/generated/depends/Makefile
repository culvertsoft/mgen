
MGEN_COMPILER_JAR = ../mgen-compiler/target/mgen-compiler-assembly-$(MGEN_BUILD_VERSION).jar

all: generate

generate:
	cd src_generated && make generate
	cd data_generated && make generate

clean:

